openapi: "3.0.3"
info:
  title: Sample YAML API
  version: "1.0.0"
servers:
  - url: https://api.example.com
paths:
  /ping:
    get:
      summary: Ping
      responses:
        "200":
          description: OK
